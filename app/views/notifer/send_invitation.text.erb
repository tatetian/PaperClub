<%= @invitor.fullname %>(<%= @invitor.email %>) invites you to join the club "<%= @club.name %>" in PaperClub.

PaperClub is a Web app that enables researchers to share, read and discuss academic papers in group.

Click the following link to start:
<%= @link %>

If you don't know <%= @invitor.fullname %>, you can ignore this email.
